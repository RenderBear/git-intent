# Optional intent expansion

Expand the raw request into a small task-specific record only when this option is enabled. Preserve
the user's prose, assign stable IDs to outcomes and acceptance criteria, state task constraints, and
leave uncertainty explicit. Do not turn implementation choices into requirements.

This record guides implementation and optional outcome review. It is local task context, not durable
repository governance.

